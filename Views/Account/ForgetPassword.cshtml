@model ForgetPasswordViewModel

@{
    ViewData["Title"] = "Forgot Password";
}

<!DOCTYPE html>
<html lang="en">
<head>
      <!-- 🔍 Metadata -->
    <meta name="description" content="BloodBridge connects blood donors and recipients efficiently across Ghana." />
    <meta name="author" content="Richard Boakye Yiadom" />
    <meta name="theme-color" content="#b30000" />

    <!-- 🧠 Favicon -->
    <link rel="icon" href="~/images/favicon.ico" type="image/x-icon" />

    <!-- 🎨 Stylesheets -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/layout.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/dashboard.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/Chartforum.css" asp-append-version="true" />

    <!-- ⚡ External Scripts -->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://unpkg.com/feather-icons"></script>

    <!-- 💡 Custom Scripts -->
    <script src="~/js/site.js" asp-append-version="true"></script>
</head>
<body>

<main class="container-fluid d-flex justify-content-center align-items-center min-vh-100">
    <div class="forgot-password-container p-4 shadow-lg rounded bg-white w-50">
        <header class="text-center mb-4">
            <h1 class="text-danger fw-bold">Reset Your Password</h1>
            <p class="text-muted">Enter your phone number to receive an OTP for verification.</p>
        </header>

        <!-- ✅ Step 1: Phone Number Input -->
        <form id="phoneVerificationForm" method="post">
            @Html.AntiForgeryToken()
            <div class="form-group mb-3">
                <label for="phoneInput" class="fw-bold">Enter Your Phone Number</label>
                <input type="tel" id="phoneInput" name="PhoneNumber" class="form-control" placeholder="Enter phone number" required />
                <span id="phoneError" class="text-danger d-none">Invalid phone number format.</span>
            </div>
            <button type="submit" class="btn btn-danger w-100 py-2 fw-bold">Send OTP</button>
        </form>

        <!-- ✅ Step 2: OTP Verification -->
        <form id="otpVerificationForm" method="post" class="mt-4 d-none">
            @Html.AntiForgeryToken()
            <div class="form-group">
                <label for="otpInput" class="fw-bold">Enter OTP</label>
                <input type="text" id="otpInput" name="Otp" class="form-control" maxlength="6" required />
                <span id="otpError" class="text-danger d-none">Invalid OTP. Try again.</span>
            </div>
            <button type="submit" class="btn btn-danger w-100 py-2 fw-bold">Verify OTP</button>
        </form>

        <p class="text-center mt-3">
            <a asp-controller="Account" asp-action="SignIn">Back to Sign In</a>
        </p>
    </div>
</main>

@section Scripts {
    <script>
        document.addEventListener("DOMContentLoaded", function () {
            const phoneForm = document.getElementById("phoneVerificationForm");
            const otpForm = document.getElementById("otpVerificationForm");
            const phoneInput = document.getElementById("phoneInput");
            const otpInput = document.getElementById("otpInput");
            const phoneError = document.getElementById("phoneError");
            const otpError = document.getElementById("otpError");

            // ✅ Validate phone format before sending OTP
            phoneForm.addEventListener("submit", function (event) {
                event.preventDefault();
                let phoneNumber = phoneInput.value.trim();
                
                if (!/^\d{10,15}$/.test(phoneNumber)) {
                    phoneError.classList.remove("d-none");
                    return;
                }
                phoneError.classList.add("d-none");

                fetch("/SendOtp", {
                    method: "POST",
                    headers: { "Content-Type": "application/x-www-form-urlencoded" },
                    body: `PhoneNumber=${phoneNumber}`
                })
                .then(response => response.ok ? response.text() : Promise.reject("⚠️ Failed to send OTP"))
                .then(data => {
                    alert("✅ OTP Sent Successfully");
                    otpForm.classList.remove("d-none");
                })
                .catch(error => {
                    alert(error);
                });
            });

            // ✅ OTP Verification Handling
            otpForm.addEventListener("submit", function (event) {
                event.preventDefault();
                let otpCode = otpInput.value.trim();

                if (!/^\d{6}$/.test(otpCode)) {
                    otpError.classList.remove("d-none");
                    return;
                }
                otpError.classList.add("d-none");

                fetch("/VerifyOtp", {
                    method: "POST",
                    headers: { "Content-Type": "application/x-www-form-urlencoded" },
                    body: `Otp=${otpCode}`
                })
                .then(response => response.ok ? response.text() : Promise.reject("⚠️ OTP Verification Failed"))
                .then(data => {
                    alert("🔑 OTP Verified Successfully! Proceed to reset your password.");
                    window.location.href = "/ResetPassword"; // Redirect to reset password page
                })
                .catch(error => {
                    alert(error);
                });
            });
        });
    </script>
}

</body>
</html>